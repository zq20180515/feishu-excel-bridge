# BTNExcel 桥 · 上架提交材料

> 本文件把飞书「提交多维表格插件」表单里每一项需要填的内容都准备好了，按顺序复制粘贴即可。
> 表单地址：https://feishu.feishu.cn/share/base/form/shrcnwTXnFVAbMPOSeaOFwIAnbf

---

## 先回答三个关键问题

### Q1：需要自己的服务器吗？

**不需要。** 这是一个**纯前端**插件——所有逻辑都在浏览器里跑，附件上传直接用多维表格 SDK 的
`bitable.base.batchUploadFile`，没有任何自建后端、不发起任何第三方 API 请求。

那"插件地址"从哪来？两条路，任选其一（建议先走 A）：

| 方式 | 地址形式 | 成本 | 适用 |
| --- | --- | --- | --- |
| **A. GitHub Pages（推荐）** | `https://<用户名>.github.io/<仓库名>/` | 免费，无需备案 | 集团内部自用 + 提交表单 |
| B. 公司内网服务器 | `https://插件域名/` | 需要一台机器 + **HTTPS 证书** | 不想依赖 GitHub |

> ⚠️ 多维表格插件**必须用 HTTPS 加载**（`localhost` 除外）。GitHub Pages 自带 HTTPS，
> 所以最省事。如果走内网自建，必须给域名配好证书，且需保证**每一位使用者都能访问到该地址**。

### Q2：`showSaveFilePicker()` 怎么测？

这是导出完成后那个「**打开文件所在位置**」按钮。**测试步骤：**

1. 把插件跑起来（本地 `npm run dev`，或已部署的线上地址）
2. 在多维表格里选一张有附件图片的数据表，正常导出一次
3. 导出完成卡片出现后，点 **「打开文件所在位置」**
4. 看结果：

| 现象 | 含义 | 是否需要处理 |
| --- | --- | --- |
| 弹出**系统的「另存为」对话框**，你选个目录点保存后，文件出现在那 | ✅ 放行了，效果最好 | 无需处理 |
| 没弹对话框，但浏览器**又下载了一次**同名文件，界面提示"请去「下载」文件夹查看" | ⚠️ 被沙箱拦了，走了降级路径 | 无需处理，功能正常，只是定位不到文件夹 |
| 弹了对话框但你点了「取消」 | 静默返回，什么都不发生 | 正常行为 |

**注意**：`showSaveFilePicker` 只在 **Chrome / Edge 86+** 支持，Firefox、Safari 不支持（会直接走降级）。
另外它还要求页面处于**安全上下文（HTTPS 或 localhost）**——这也是必须上 HTTPS 的另一个原因。

> 好消息：这个按钮**不影响上架审核**。它只是导出后的一个便利入口，被拦了也只是少点一下"去下载文件夹"。

### Q3：表单里没有单独的「GitHub 地址」栏

按截图，表单里那个提示"如果需要在 GitHub 页更新插件代码，可以填此处"的输入框，
**就是让你填仓库地址用的**（用来关联代码仓库、便于后续更新）。

**这一栏是选填的**——填了能加速审核（审核方可以直接看代码），建议填。

---

## 表单逐项填写内容

### ① 插件类型 `*必填`

**勾选「边栏插件」**（其他项不要勾）

> ✅ 边栏插件 = 在多维表格右侧侧边栏打开。本插件就是侧边栏形态。

---

### ② 发布范围 `*必填`

**勾选「提交人所在公司内部可见」**

> 集团内部使用选这个。若之后想开放给全量用户，再重新提交一次选"全量发布"。

---

### ③ 插件名称 `*必填`

```
BTNExcel 桥
```

---

### ④ 开发者名称 `*必填`

```
张强
```

> 建议填真实姓名，审核方可能会联系。如需带部门：`张强（贝泰妮信息技术部）`

---

### ⑤ 功能描述 `*必填`

> 这段会展示在插件市场，是用户第一眼看到的东西。直接复制：

```
一键连通 Excel 与多维表格，重点解决官方原生能力无法处理的「图片」问题。

【导入】把本地 Excel 拖进来，字段一一映射写入多维表格。Excel 里那些导入后变成
=DISPIMG("ID_xxx",1) 文本而丢失的图片，会被自动识别并转成多维表格的「附件」字段。
支持多工作表、单选/多选、追加到已有数据表，可增删改字段与类型。

【导出】多选数据表导出为 Excel，附件里的图片会重新嵌回单元格，两种方式可选：
WPS 嵌入单元格图片、标准浮动图片。附件字段有多张图时可自动展开为
「照片 / 照片2 / 照片3」多列。

【特点】纯前端插件，无后端、不上传任何数据到第三方服务器；图片批量上传走串行队列，
不会触发多维表格接口的并发限制。
```

---

### ⑥ 插件使用说明 `*必填`

> 面向使用者的操作指引，写清楚怎么用。直接复制：

```
── 安装 ──
方式一（推荐）：多维表格右上角「插件」→ 底部「自定义插件」→「+ 新增插件」，
填入插件地址后确定。
方式二：在插件市场搜索「BTNExcel 桥」直接添加。

── 导入 Excel ──
1. 切到「导入 Excel」标签，把 .xlsx 等表格文件拖进虚线框（或点击选择）
2. 插件会列出文件里所有工作表的字段映射关系，每行是「字段名称 → 字段类型」
3. 按需调整：
   · 取消勾选某一行 = 不导入该字段
   · 点右侧的类型胶囊可换字段类型
   · 点工作表名可折叠/展开，也可改导入后的数据表名称
   · 选择「追加到已有数据表」时，还能把列映射到目标表里已有的字段
4. 点右下角「开始导入」，结束后会给出每张表的写入条数

── 导出为 Excel ──
1. 切到「导出为 Excel」标签，勾选要导出的数据表（可多选）
2. 选图片嵌入方式：
   · WPS嵌入单元格图片 —— 图片真正待在单元格里，需用 WPS 打开查看
   · 标准浮动图片 —— Excel / WPS / LibreOffice 都能显示，兼容性最好
3. 可选：填「图片边长」等比缩放（留空 = 原图原尺寸）；
   「全部图片都导出」默认开启，会把每个附件里的多张图展开成多列
4. 点「导出并下载」，完成后可直接点「再下载一次」或「打开文件所在位置」

── 常见问题 ──
· 导入后图片列是空的？说明文件格式不含图片部件（如 .xls / .csv），
  请先用 WPS 或 Excel 另存为 .xlsx 再导入。插件在解析时也会给出提示。
· 导入会不会写坏我原来的表？导入是真实写库操作，建议先新建一个空的多维表格试一次。
  同名数据表会自动加序号新建，同名已有字段会复用而不是重复新建。
· 导出后 Excel 里显示 =DISPIMG(...) 公式？那是 WPS 的专有扩展，
  改用「标准浮动图片」模式即可在原生 Excel 里看到图片。
· WPS 嵌入模式下图片能调大小吗？可以，「图片边长」对两种模式都生效。

── 反馈 ──
插件名右侧有「反馈」按钮，可一键复制反馈信息（含插件版本号），
在飞书里 @张强 并附上截图即可。
```

---

### ⑦ 图标【非必须，留空则为系统默认】

**可选。** 留空则用系统默认图标。

如果要传，建议：
- 尺寸 **512 × 512**，PNG，透明背景
- 内容建议：一个桥接/双向箭头的简约图形，或 Excel 表格与多维表格关联的意象
- 主色可沿用插件主色 `#0c6b63`（深青）

> 需要的话我可以给你生成一版图标。

---

### ⑧ 是否上传插件配图 / 视频？【多选，非必须】

**建议勾选「中文」并上传配图**（有配图的插件通过率和使用率都更高）。

需要准备的截图（在多维表格里实际操作并截图）：

| 序号 | 截图内容 | 建议文件名 |
| --- | --- | --- |
| 1 | 导入标签页，拖入 Excel 后的**字段映射区**（展示字段名→类型色块） | `01-导入映射.png` |
| 2 | 导入完成后的**结果卡片**（各表写入条数） | `02-导入结果.png` |
| 3 | 导出标签页，**数据表多选 + 导出选项** | `03-导出选项.png` |
| 4 | 导出完成，展示**图片已嵌入单元格**的 Excel（用 WPS 打开截图最有说服力） | `04-导出效果.png` |

> 想偷懒可以直接用仓库 `samples/` 目录下那 4 个样例 xlsx 截图，
> 其中 `导出效果_WPS内嵌图.xlsx` 用 WPS 打开就是嵌图效果。

---

### ⑨ 插件使用是否收费？ `*必填`

**勾选「不收」**

---

### ⑩ 其他信息

> 这里是自由填写。截图里那段说明意思是：如果代码托管在 GitHub，可以填仓库地址，
> 方便审核方查看与后续更新。**建议填**，能加速审核。

```
GitHub 代码仓库：https://github.com/<你的用户名>/feishu-excel-bridge

说明：
1. 本插件为纯前端实现，无自建后端服务，不向任何第三方服务器发送数据。
   所有 Excel 解析、图片提取与打包均在浏览器本地完成；
   附件上传使用多维表格官方 SDK 的 bitable.base.batchUploadFile（串行调用，遵守官方并发限制）。
2. 未使用任何需要额外授权的 OpenAPI 权限，仅依赖前端 SDK 的表格读写能力。
3. 项目内含 151 项自动化断言（round-trip 92 项 + DOM 渲染 59 项），
   GitHub Actions 在每次推送时自动跑测试并部署，可据此复核代码质量。
4. 已用真实 WPS 产出的含图工作簿做过结构比对（test/compare-wps.py），
   确认写入的 DISPIMG 结构与 WPS 原生输出一致。
```

> ⚠️ 记得把 `<你的用户名>` 换成实际的 GitHub 用户名。

---

## 接下来要做的三件事（按顺序）

### 第 1 步：部署到 GitHub Pages

详见 `DEPLOY.md`。核心就三步：建仓库 → push → 开启 Pages。
完成后你会得到一个地址，形如：

```
https://<你的用户名>.github.io/feishu-excel-bridge/
```

### 第 2 步：自测这个线上地址

1. 多维表格 →「插件」→「自定义插件」→「+ 新增插件」
2. 名称填 `BTNExcel 桥`，地址填上面那个 GitHub Pages 地址
3. 确定后打开插件，把 `samples/示例源表_内嵌图.xlsx` 拖进去跑一遍导入
4. 再选一张有图的数据表跑一遍导出，用 WPS 打开确认图片在单元格里

> ⚠️ 多维表格的「自定义插件」地址**必须是 HTTPS**。GitHub Pages 默认就是 HTTPS，没问题。

### 第 3 步：提交表单

按上面 ① ~ ⑩ 逐项复制粘贴，上传配图截图，提交后等审核。
审核通过后，集团内其他同事就能在多维表格的插件市场里搜到并安装了。
