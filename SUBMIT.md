# BTNExcel 桥 · 上架提交材料（对齐真实表单）

> 本文件按**实际表单**（12 项）逐项整理，可直接复制。  
> 表单地址：<https://feishu.feishu.cn/share/base/form/shrcnwTXnFVAbMPOSeaOFwIAnbf>  
> 审核周期：**3 个工作日**

---

## ⚠️ 先看：4 处容易填错的地方

我按表单原文逐项核对过，这几项**和直觉不一样**：

| 项           | 坑                                   |
| ----------- | ----------------------------------- |
| ⑤ 功能描述      | **只允许 1-15 字！** 不是长段落。写成 200 字会被打回  |
| ⑥ 插件使用说明    | 支持 **Markdown**，推荐列 2-5 条亮点 + 使用指南  |
| ⑦ Github 地址 | **必填**（红框），且有 3 个硬性条件（见下）           |
| ⑧ 插件模板      | **要在多维表格里做一个含 5-10 行演示数据的模板**，不是传截图 |
| ⑩ 配图        | 这个才是传截图的地方（选填）                      |

---

## ✅ ⑦ Github 地址的三个硬性条件（已全部验证通过）

| 条件                                                              | 你的情况                                     | 状态 |
| --------------------------------------------------------------- | ---------------------------------------- | -- |
| 1. GitHub 邀请 `bitable-open@bytedance.com` **或** 仓库设为 **public** | `visibility: public`                     | ✅  |
| 2. **index.html 必须在 dist 根目录**                                  | `dist/index.html` 存在                     | ✅  |
| 3. 资源引用使用**相对路径**                                               | `./assets/index.js`、`./assets/index.css` | ✅  |

**结论：这一栏直接填下面的地址即可，无需任何改动。**

```
https://github.com/zq20180515/feishu-excel-bridge
```

> 💡 表单这里要的是 **GitHub 仓库地址**（不是 Pages 地址）。  
> 仓库是 public，所以不需要额外邀请 `bitable-open@bytedance.com`。  
> 若你之后把仓库改成 private，则**必须**去 `Settings → Collaborators` 邀请该邮箱。

---

# 逐项填写内容

## ① 插件类型 `*必填`

**勾选「边栏插件」**（只勾这一项）

> 边栏插件 = 在多维表格右侧侧边栏打开，本插件就是侧边栏形态。

---

## ② 发布范围 `*必填`

**勾选「提交人所在公司内部可见」**

---

## ③ 插件名称 `*必填`

```
BTNExcel 桥
```



> ⚠️ 表单提示：**插件名称过短、过长、或与已有插件重名都可能搜不到**；  
> 不允许只用模型名命名；**上架后请搜索全称使用**。
>
> `BTNExcel 桥` 长度适中且有辨识度（带公司前缀 BTN），符合要求。

---

## ④ 开发者名称 `*必填`

```
张强
```

> 建议填真实姓名，审核方可能联系。如需带部门：`张强（贝泰妮信息技术部）`

---

## ⑤ 功能描述 `*必填` 【1-15 字，必须短】

**推荐用这条（13 字，含空格）：**

```
Excel 图片导入导出桥
```

备选（都在 15 字内）：

| 文案               | 字数 |
| ---------------- | -- |
| `Excel 图片直连多维表格` | 14 |
| `Excel 图片导入导出助手` | 14 |
| `Excel 带图导入导出`   | 12 |
| `Excel 图片双向桥`    | 11 |

> ❌ **不要**把下面 ⑥ 的长文案粘到这里，字段限制 15 字，会被截断或直接打回。

---

## ⑥ 插件使用说明 `*必填` 【支持 Markdown】

> 表单原文：「展示于插件详情页，可以更详细的介绍该插件的亮点功能。  
> **推荐列举 2-5 条重要的功能，以及简要的使用指南。** 【排版请使用 Markdown 格式】」

**直接复制（已是 Markdown 排版）：**

```markdown
**一键打通 Excel 与多维表格的图片难题。** 官方原生导入会把图片变成
`=DISPIMG("ID_xxx",1)` 文本、导出则图片进不了单元格 —— 本插件解决这两个问题。

## 核心功能

- **图片导入不丢失**：把 Excel 拖进来，里面的图片/视频自动识别并转成多维表格的**附件字段**，字段一一映射到位。
- **图片导出回单元格**：附件里的图片重新嵌回单元格，支持「WPS 嵌入单元格图片」与「标准浮动图片」两种方式。
- **多工作表 / 多图片全支持**：多个 sheet 各自导入为独立数据表；附件有多张图时自动展开为「照片 / 照片2 / 照片3」多列。
- **纯前端、无后端**：所有解析与打包在浏览器本地完成，不上传任何数据到第三方服务器。

## 使用指南

### 导入 Excel

1. 切到「导入 Excel」标签，把 `.xlsx` 等表格文件拖进虚线框（或点击选择）
2. 插件列出文件里所有工作表的字段映射关系，每行是「字段名称 → 字段类型」
3. 按需调整：取消勾选某行 = 不导入该字段；点右侧类型胶囊可换字段类型；点工作表名可折叠或改表名
4. 点右下角「开始导入」，结束后会给出每张表的写入条数

### 导出为 Excel

1. 切到「导出为 Excel」标签，勾选要导出的数据表（可多选）
2. 选图片嵌入方式：
   - **WPS嵌入单元格图片** —— 图片真正待在单元格里，需用 WPS 打开查看
   - **标准浮动图片** —— Excel / WPS / LibreOffice 都能显示，兼容性最好
3. 可选：填「图片边长」等比缩放（留空 = 原图原尺寸）；「全部图片都导出」默认开启
4. 点「导出并下载」

## 常见问题

- **导入后图片列是空的？** 说明文件格式不含图片部件（如 `.xls` / `.csv`），请先用 WPS 或 Excel 另存为 `.xlsx` 再导入。
- **导出后 Excel 里显示公式？** 那是 WPS 的专有扩展，改用「标准浮动图片」模式即可在原生 Excel 里看到图片。
- **会不会写坏原来的表？** 导入是真实写库操作，建议先新建一个空的多维表格试一次。

## 反馈

插件名右侧有「反馈」按钮，可一键复制反馈信息（含版本号），在飞书里 @张强 并附截图即可。
```

---

## ⑦ Github 地址 `*必填`

```
https://github.com/zq20180515/feishu-excel-bridge
```

> 三个条件已验证通过，详见本文开头的表格。  
> 注意这栏要的是**仓库地址**，不是 Pages 地址。

---

## ⑧ 插件模板【需在飞书里操作】

> 表单原文：「请在一个新的多维表格中**制作一个模板，提供 5-10 行能演示该插件的数据**，  
> 后续用户也可以使用这个模板来体验插件功能。模板的链接注意开启『互联网知链接可访问』」

**这是本次唯一需要你在飞书里动手做的一步。** 步骤如下：

### 制作模板（约 5 分钟）

1. **新建一个多维表格**，命名为 `BTNExcel 桥 · 体验模板`
2. **建一张数据表**，命名为 `订单样例`，按下表填入 **5-10 行演示数据**：
   | 订单号  | 客户   | 产品  | 数量 | 金额   | 照片            |
   | ---- | ---- | --- | -- | ---- | ------------- |
   | A001 | 昆明百货 | 面霜  | 12 | 3600 | （附件字段，传 1 张图） |
   | A002 | 云南药业 | 面膜  | 30 | 4500 | （附件字段，传 1 张图） |
   | A003 | 呈贡商超 | 精华液 | 8  | 2400 | （附件字段，传 1 张图） |
   | A004 | 曲靖连锁 | 洗面奶 | 20 | 1800 | （附件字段，传 1 张图） |
   | A005 | 玉溪商贸 | 防晒霜 | 15 | 2250 | （附件字段，传 1 张图） |
   > 字段类型这样设：订单号=文本、客户=文本、产品=文本、数量=数字、金额=数字、  
   > **照片=附件**（关键，用来演示图片导出）
3. **把「照片」列填上真实的图片附件**（随便传几张图片即可，用于演示导出嵌图）

### 设置分享

1. 右上角 **分享** → 把权限改成 **「互联网上获得链接的人可阅读」**
2. 复制链接，填进表单第 ⑧ 项

> ⚠️ **必须开启「互联网获得链接可访问」**，否则审核同学打不开，会被打回。

**填进表单的内容格式：**

```
BTNExcel 桥 体验模板：<把你在飞书复制的多维表格分享链接粘到这里>
```

---

## ⑨ 图标【非必须，留空则为系统默认】

**已备好**：`assets/icon-512.png`（512×512 PNG，**透明背景**，5.4 KB）

![](assets/icon-512.png)

> **设计**：无底板，透明画布上直接是「一对主色双向箭头 + 主色表格卡片」。
> 内容占满画布 **82% × 77%**（四周留白 45/45/63/56 px），
> 插件市场做圆形或圆角裁切时不会切到内容。
> 箭头用**实心主色** `#0c6b63`，在**白底和深色底上都清晰**。
>
> 重新生成：`python test/make-icon.py assets/icon-512.png 512`
> 自检预览：`python test/preview-icon.py assets/icon-512.png .iconpreview/sheet.png --sheet`
> （输出包围盒/填充率/四边留白，并生成 浅底·深底·棋盘格 三联对比图）

---

## ⑩ 是否上传插件配图 / 视频？【多选，非必须】

**勾选「中文」**（可选/多选，建议只勾中文，因为面向集团内部）。

需要准备的截图（**这一项才是传图的地方**）：

**方案 A（最快）**：直接用仓库 `samples/` 里现成的 xlsx 截图

| 序号 | 用什么截                       | 怎么截                  |
| -- | -------------------------- | -------------------- |
| 1  | `samples/示例源表_内嵌图.xlsx`    | Excel/WPS 打开 → 全窗口截图 |
| 2  | `samples/示例源表_浮动图.xlsx`    | 同上                   |
| 3  | `samples/导出效果_WPS内嵌图.xlsx` | **用 WPS 打开**，图片在单元格里 |
| 4  | `samples/导出效果_浮动图片.xlsx`   | Excel 打开即可           |

**方案 B（推荐，更有说服力）**：在真实多维表格里操作截图

| 序号 | 截图内容                       | 建议文件名         |
| -- | -------------------------- | ------------- |
| 1  | 导入标签页，拖入 Excel 后的**字段映射区** | `01-导入映射.png` |
| 2  | 导入完成后的**结果卡片**（各表写入条数）     | `02-导入结果.png` |
| 3  | 导出标签页，**数据表多选 + 导出选项**     | `03-导出选项.png` |
| 4  | 多维表格里图片已进**附件字段**的样子       | `04-附件字段.png` |

> 配图建议：宽度 ≥ 800px 的 PNG/JPG，单张 ≤ 5MB。  
> 侧边栏较窄，截图时把多维表格窗口拉宽一点效果更好。

---

## ⑪ 插件使用是否收费？ `*必填`

**勾选「不收」**

---

## ⑫ 其他信息

**直接复制（地址已填好）：**

```
GitHub 代码仓库：https://github.com/zq20180515/feishu-excel-bridge
在线体验地址：https://zq20180515.github.io/feishu-excel-bridge/

说明：
1. 本插件为纯前端实现，无自建后端服务，不向任何第三方服务器发送数据。所有 Excel 解析、图片提取与打包均在浏览器本地完成；附件上传使用多维表格官方 SDK 的 bitable.base.batchUploadFile（全局串行调用，遵守官方并发限制）。
2. 未使用任何需要额外授权的 OpenAPI 权限，仅依赖前端 SDK 自身的表格读写能力，打开插件时不会弹出额外授权申请。
3. 代码仓库内含 151 项自动化断言（Excel 读写往返 92 项 + DOM 渲染 59 项），GitHub Actions 每次推送时自动跑测试并部署，可据此复核代码质量；本地可执行 npm test 复现。
4. 已用真实 WPS 产出的含图工作簿做过结构比对（test/compare-wps.py），确认写入的图片结构与 WPS 原生输出一致。
5. 本插件为本人（张强）在集团内部工作中自研，用于打通 Excel 与多维表格之间长期存在的"图片无法导入 / 无法嵌入单元格"问题，无商业目的。
```

---

# 提交前检查清单

| # | 检查项                                               | 状态 |
| - | ------------------------------------------------- | -- |
| 1 | 插件地址填的是**仓库地址**（不是 Pages 地址）                      | ⬜  |
| 2 | 仓库是 **public**（或已邀请 `bitable-open@bytedance.com`） | ✅  |
| 3 | `dist/index.html` 在根目录、资源用相对路径                    | ✅  |
| 4 | ⑤ 功能描述**控制在 15 字内**                               | ⬜  |
| 5 | ⑧ **已在飞书做好体验模板**并开启「互联网获得链接可访问」                   | ⬜  |
| 6 | ⑨ 图标已上传 `assets/icon-512.png`                     | ⬜  |
| 7 | ⑩ 配图已上传（勾选了「中文」）                                  | ⬜  |
| 8 | 已在飞书「自定义插件」用线上地址实测过导入 + 导出                        | ⬜  |

---

# 上架后

审核通过后，集团同事在多维表格插件市场**搜索全称 `BTNExcel 桥`** 即可找到并安装。

> 表单提示：「插件上架后，请搜索全称使用」—— 因为插件搜索是向量搜索，  
> 简称可能搜不到，告诉同事搜全称。
