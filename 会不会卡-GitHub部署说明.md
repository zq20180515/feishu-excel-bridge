# 部署在 GitHub Pages 会影响插件速度和稳定性吗？

> 结论先说：**日常使用几乎没影响。只有「打开插件的那一刻」需要从 CDN 下载约 500 KB 代码，
> 之后就完全和 GitHub 无关了。** 而且国内访问 `*.github.io` 比访问 `github.com` 顺畅得多。

---

## 一、先分清两件事：`github.com` ≠ `*.github.io`

这是最容易混淆的地方，也是你之前 `git push` 失败的根源：

| 域名 | 用途 | 国内访问情况 |
| --- | --- | --- |
| **`github.com`** | 代码托管、git push/pull | ⚠️ 不稳定，经常被重置（你踩过的坑） |
| **`*.github.io`** | **Pages 静态站点（插件实际加载的）** | ✅ 走 **Fastly CDN**，通常可直连 |

**关键**：插件加载走的是 `*.github.io`，**不是** `github.com`。
两者解析到完全不同的 IP：

```
github.com        → 140.82.121.4        （GitHub 自建机房）
xxx.github.io     → 185.199.108.153     （Fastly CDN，4 个 IP 轮询）
                    185.199.109.153
                    185.199.110.153
                    185.199.111.153
```

`185.199.x.x` 是 Fastly 的边缘节点，国内多数网络可直连，实测延迟 **0.2~0.5 秒**。

> 你之前 push 失败是因为 git 要连 `github.com`；
> 而同事用插件时连的是 `github.io` —— **两码事**。

---

## 二、插件的网络依赖（源码实测）

我扫了整个 `src/` 目录，**全部网络请求只有一处**：

```ts
// src/lib/exporter.ts:233 —— 导出时下载飞书附件图片
const res = await fetch(url)     // url 来自 getAttachmentUrls()，是飞书自己的临时链接
```

除此之外：
- 没有任何第三方 API 调用
- 没有 CDN 字体、没有统计脚本、没有外部图片
- Excel 解析、图片提取、ZIP 打包**全在浏览器本地完成**

**所以运行时依赖的外部服务只有两个：**
1. `*.github.io` —— 只在**打开插件时**加载代码
2. 飞书自己的服务器 —— 读写表格、上传下载附件

---

## 三、实测数据

从你部署的站点量出来的真实数字：

| 项目 | 数值 |
| --- | --- |
| 首页 HTML | **412 字节** |
| JS（gzip 后） | **510 KB** |
| CSS（gzip 后） | **22 KB** |
| **首次加载总计** | **约 530 KB** |
| CDN 响应时间 | **0.2~0.5 秒** |
| 缓存头 | `Cache-Control: max-age=600`、`X-Cache: HIT` |

> 530 KB 是什么概念：大约等于**一张手机照片的十分之一**，
> 比打开一个普通企业网站还轻。

---

## 四、实际使用中的三个阶段

### 阶段 1：首次打开插件

需要从 CDN 下载 530 KB。国内网络正常的话，**1 秒内完成**。
如果网络特别差，最多也就两三秒 —— 用户看到的是侧边栏里的加载过程，不影响飞书主界面。

### 阶段 2：浏览器缓存命中（同一会话内再次打开）

代码已在浏览器缓存里（`max-age=600`，10 分钟），**基本是瞬开**。

### 阶段 3：实际使用（导入 / 导出）

**完全不碰 GitHub。** 数据流是：

```
用户浏览器 ──→ 飞书服务器（读写表格、上传附件）
```

Excel 解析在本地 CPU 上跑，图片打包在本地内存里做。
**这一步的速度取决于：文件大小、飞书接口响应、你的电脑性能 —— 与 GitHub 无关。**

---

## 五、会不会卡？

**不会。** 理由：

1. **代码只有 530 KB 且只加载一次**，不是每次操作都请求
2. **真正的重活（解析 Excel、处理图片）都在本地跑**，走的是用户自己的 CPU
3. **批量操作有串行队列 + 进度反馈**（`.prog` 进度块），不会因为网络抖动就卡住
4. **附件上传/下载走的是飞书域名**，和 GitHub 完全无关

唯一可能感知到 GitHub 的场景：**首次加载时网络极差**。解决办法是缓存，第二次就没了。

---

## 六、如果还是担心，有两条路

### 路线 A：什么都不用改（推荐）

先按现状上架。理由是：
- 数据上看，国内访问 `github.io` 正常
- 530 KB 首次加载，实际体验影响很小
- 集团内部用户都在同一网络环境，**上线后收集真实反馈再决定**

### 路线 B：换成公司内网 / 国内静态托管（如果实测确实慢）

本项目 `dist/` 是纯静态产物，**换托管不用改一行代码**：

```bash
npm run build     # 产出 dist/
```

把 `dist/` 整个目录扔到任意静态托管即可。可选：

| 方案 | 说明 |
| --- | --- |
| 公司内网服务器 | 用 Nginx 托管 `dist/`，**必须配 HTTPS 证书** |
| 腾讯云 COS / 阿里云 OSS | 静态网站托管 + CDN，国内速度快 |
| Gitee Pages | 国内直连，但需实名认证 + 手动部署 |

换完只需**改一个地方**：在飞书「自定义插件」里把地址换成新地址。
（`vite.config.ts` 用的是相对路径 `base: './'`，任何域名/子路径都能直接用。）

### 上架后的判断标准

如果同事反馈「打开插件要等好几秒」，那就走路线 B。
如果反馈是「导入大文件慢」—— 那是**飞书接口或本地性能**问题，
换托管不会改善，需要改的是分批策略。

---

## 七、一句话总结

> **只有「打开插件」这一步依赖 GitHub CDN，约 530 KB、通常不到 1 秒，且有浏览器缓存。
> 之后的导入导出全部在本地 + 飞书服务器之间完成，与 GitHub 无关。**
>
> 真要优化，优化的对象也是飞书接口和本地处理，而不是 GitHub Pages。
